export async function runCalculator(apiKey, match) {
    console.warn('runCalculator ainda não implementado');

    return {
        players: 'Aguardando implementação',
        corners: 'Aguardando implementação',
        cards: 'Aguardando implementação'
    };
}
